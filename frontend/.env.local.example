# Backend API base URL (server-side only; the browser never calls it directly)
API_URL=http://192.168.1.200:8000

# Public app URL (for redirects, emails)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Cookie security: set to "true" in production (HTTPS required)
COOKIE_SECURE=false

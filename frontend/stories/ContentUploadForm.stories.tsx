import { ContentUploadForm } from '@/components/forms/ContentUploadForm';

const meta = {
  title: 'Forms/ContentUploadForm',
  component: ContentUploadForm,
  parameters: { layout: 'padded' },
};
export default meta;

export const Default = {};
